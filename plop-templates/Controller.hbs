/**
 * Controller untuk {{pascalCase name}}
 * @module controllers/{{camelCase name}}
 * @requires ../models/{{pascalCase name}}
 * @requires express-validator
 */

const { {{pascalCase name}} } = require("../models");
const { validationResult } = require("express-validator");

exports.index = async (req, res) => {
  // Tampilkan daftar {{camelCase name}}
};

exports.create = (req, res) => {
  // Tampilkan form tambah {{camelCase name}}
};

exports.store = async (req, res) => {
  // Simpan data baru {{camelCase name}}
};

exports.edit = async (req, res) => {
  // Tampilkan form edit {{camelCase name}}
};

exports.update = async (req, res) => {
  // Update data {{camelCase name}} berdasarkan ID
};

exports.destroy = async (req, res) => {
  // Hapus data {{camelCase name}} berdasarkan ID
};
