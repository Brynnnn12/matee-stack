/**
 * {{camelCase name}} middleware
 *
 * Cara penggunaan di Express:
 *
 * const {{camelCase name}} = require('../middlewares/{{camelCase name}}');
 * app.use({{camelCase name}});
 *
 * // Atau untuk route tertentu:
 * app.get('/route', {{camelCase name}}, (req, res, next) => { ... });
 */

module.exports = (req, res, next) => {
  // Logika middleware {{camelCase name}} di sini
  next();
};
